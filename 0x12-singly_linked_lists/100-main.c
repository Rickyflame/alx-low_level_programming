#include <stdio.h>

/**
 * main - checks the code
 * Return: Always 0
 */
int main(void)
{
	printf("(A tortiose, having pretty good sense of a hare's nature, challenges one to a race.)\n");
	return (0);
}
